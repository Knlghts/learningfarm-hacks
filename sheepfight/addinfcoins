if (confirm("To see your coins you must spend one coin")) {
  txt = "You pressed OK!";
    let n = window.sheepGame.coins;
window.sheepGame.coins = n + 1000; // Adds 20 coins to the existing value
} else {
  txt = "You pressed Cancel!";
}
